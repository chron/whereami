<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>whereami</title>
  <link rel="stylesheet" href="css/index.css">
</head>
<body>
  <h1>#where-am-i</h1>

  <div class="emoji-grid">
    <div class="emoji-grid__header">Name</div>
    {% for weekNum in weeks %}
      <div class="emoji-grid__header">Week {{ weekNum }}</div>
    {% endfor %}

    {% for user in slack %}
      <div class="emoji-grid__name">
        {{ user.name }}
      </div>

      {% for week in user.weeks %}
        <div class="emoji-grid__week">
          {% for e in week.emojis %}
            {%- parseEmoji emoji, e -%}
          {% endfor %}
        </div>
      {% endfor %}
    {% endfor %}
  </div>
</body>
</html>
